name: "AirVpn"
package_name: "com.airvpn.secure"
version: "1.0.0"

android:
  permissions:
    - INTERNET
    - CAMERA
    - WRITE_EXTERNAL_STORAGE
    - READ_EXTERNAL_STORAGE
    - FOREGROUND_SERVICE
    - WAKE_LOCK
  arch: arm64-v8a
